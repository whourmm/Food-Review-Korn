"""Configuration for the RAI application."""
from django.apps import AppConfig

class RaiConfig(AppConfig):
    """Configuration for the RAI application."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'food_review_korn'